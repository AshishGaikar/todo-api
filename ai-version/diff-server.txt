diff --git a/server.js b/ai-version/server.js
index 0d7bf1f..e2ab009 100644
--- a/server.js
+++ b/ai-version/server.js
@@ -1,167 +1,76 @@
-// Task API — a small in-memory CRUD API for managing a to-do list.
-// FlyRank Internship · Backend Track · W2 · A1
-
 const express = require("express");
 const swaggerUi = require("swagger-ui-express");
-const openapiSpec = require("./openapi.json");
 
 const app = express();
 const PORT = 3000;
 
 app.use(express.json());
 
-// ---------------------------------------------------------------------------
-// Stage 2: in-memory "database" — just a list in code, pre-filled with 3 tasks
-// ---------------------------------------------------------------------------
-const SEED_TASKS = [
-  { id: 1, title: "Buy milk", done: false },
-  { id: 2, title: "Write README", done: false },
-  { id: 3, title: "Walk the dog", done: true },
+let tasks = [
+  { id: 1, title: "Sample task", done: false },
 ];
+let nextId = 2;
+
+// Swagger UI, generated from a minimal inline spec
+const swaggerDocument = {
+  openapi: "3.0.0",
+  info: { title: "Task API", version: "1.0.0" },
+  paths: {
+    "/tasks": {
+      get: { summary: "Get all tasks", responses: { 200: { description: "OK" } } },
+      post: { summary: "Create a task", responses: { 201: { description: "Created" } } },
+    },
+    "/tasks/{id}": {
+      get: { summary: "Get a task", responses: { 200: { description: "OK" }, 404: { description: "Not found" } } },
+      put: { summary: "Update a task", responses: { 200: { description: "OK" } } },
+      delete: { summary: "Delete a task", responses: { 200: { description: "OK" } } },
+    },
+  },
+};
+app.use("/docs", swaggerUi.serve, swaggerUi.setup(swaggerDocument));
 
-let tasks = SEED_TASKS.map((t) => ({ ...t }));
-let nextId = tasks.length + 1;
-
-// ---------------------------------------------------------------------------
-// Stage 1: root + health endpoints
-// ---------------------------------------------------------------------------
-app.get("/", (req, res) => {
-  res.status(200).json({
-    name: "Task API",
-    version: "1.0",
-    endpoints: ["/tasks", "/tasks/:id", "/health", "/stats", "/reset", "/docs"],
-  });
-});
-
-app.get("/health", (req, res) => {
-  res.status(200).json({ status: "ok" });
-});
-
-// ---------------------------------------------------------------------------
-// Extras (optional, stretch): stats + reset. Defined before /tasks/:id so
-// they aren't swallowed by the :id route.
-// ---------------------------------------------------------------------------
-app.get("/stats", (req, res) => {
-  const total = tasks.length;
-  const done = tasks.filter((t) => t.done).length;
-  res.status(200).json({ total, done, open: total - done });
-});
-
-app.post("/reset", (req, res) => {
-  tasks = SEED_TASKS.map((t) => ({ ...t }));
-  nextId = tasks.length + 1;
-  res.status(200).json({ status: "reset", tasks });
-});
-
-// ---------------------------------------------------------------------------
-// Stage 2: Read — list and single task
-// Extras: ?done=true|false filter, ?search=word filter, ?limit=&offset=
-// ---------------------------------------------------------------------------
 app.get("/tasks", (req, res) => {
-  let result = tasks;
-
-  if (req.query.done !== undefined) {
-    const wantDone = req.query.done === "true";
-    result = result.filter((t) => t.done === wantDone);
-  }
-
-  if (req.query.search) {
-    const needle = req.query.search.toLowerCase();
-    result = result.filter((t) => t.title.toLowerCase().includes(needle));
-  }
-
-  if (req.query.limit !== undefined || req.query.offset !== undefined) {
-    const offset = parseInt(req.query.offset, 10) || 0;
-    const limit = req.query.limit !== undefined ? parseInt(req.query.limit, 10) : result.length;
-    result = result.slice(offset, offset + limit);
-  }
-
-  res.status(200).json(result);
+  res.json(tasks);
 });
 
 app.get("/tasks/:id", (req, res) => {
-  const id = parseInt(req.params.id, 10);
-  const task = tasks.find((t) => t.id === id);
-
+  const task = tasks.find((t) => t.id === parseInt(req.params.id));
   if (!task) {
-    return res.status(404).json({ error: `Task ${req.params.id} not found` });
+    return res.status(404).json({ error: "Task not found" });
   }
-
-  res.status(200).json(task);
+  res.json(task);
 });
 
-// ---------------------------------------------------------------------------
-// Stage 3: Create
-// ---------------------------------------------------------------------------
 app.post("/tasks", (req, res) => {
-  const { title } = req.body || {};
-
-  if (!title || typeof title !== "string" || title.trim() === "") {
-    return res.status(400).json({ error: "title is required and must be a non-empty string" });
+  if (!req.body.title) {
+    return res.status(400).json({ error: "Title is required" });
   }
-
-  const newTask = { id: nextId++, title: title.trim(), done: false };
-  tasks.push(newTask);
-  res.status(201).json(newTask);
+  const task = { id: nextId++, title: req.body.title, done: false };
+  tasks.push(task);
+  res.status(201).json(task);
 });
 
-// ---------------------------------------------------------------------------
-// Stage 4: Update & Delete
-// ---------------------------------------------------------------------------
+// Note: no body validation here — assumes the client sends a well-formed body.
 app.put("/tasks/:id", (req, res) => {
-  const id = parseInt(req.params.id, 10);
-  const task = tasks.find((t) => t.id === id);
-
+  const task = tasks.find((t) => t.id === parseInt(req.params.id));
   if (!task) {
-    return res.status(404).json({ error: `Task ${req.params.id} not found` });
-  }
-
-  const { title, done } = req.body || {};
-
-  const titleProvided = title !== undefined;
-  const doneProvided = done !== undefined;
-
-  if (!titleProvided && !doneProvided) {
-    return res.status(400).json({ error: "request body must include title and/or done" });
-  }
-  if (titleProvided && (typeof title !== "string" || title.trim() === "")) {
-    return res.status(400).json({ error: "title must be a non-empty string" });
+    return res.status(404).json({ error: "Task not found" });
   }
-  if (doneProvided && typeof done !== "boolean") {
-    return res.status(400).json({ error: "done must be a boolean" });
-  }
-
-  if (titleProvided) task.title = title.trim();
-  if (doneProvided) task.done = done;
-
-  res.status(200).json(task);
+  if (req.body.title !== undefined) task.title = req.body.title;
+  if (req.body.done !== undefined) task.done = req.body.done;
+  res.json(task);
 });
 
+// Note: returns 200 with the deleted task instead of 204 + empty body.
 app.delete("/tasks/:id", (req, res) => {
-  const id = parseInt(req.params.id, 10);
-  const index = tasks.findIndex((t) => t.id === id);
-
+  const index = tasks.findIndex((t) => t.id === parseInt(req.params.id));
   if (index === -1) {
-    return res.status(404).json({ error: `Task ${req.params.id} not found` });
+    return res.status(404).json({ error: "Task not found" });
   }
-
-  tasks.splice(index, 1);
-  res.status(204).send();
-});
-
-// ---------------------------------------------------------------------------
-// Stage 5: Swagger UI at /docs, generated from openapi.json
-// ---------------------------------------------------------------------------
-app.use("/docs", swaggerUi.serve, swaggerUi.setup(openapiSpec));
-
-// ---------------------------------------------------------------------------
-// Fallback 404 for unknown routes
-// ---------------------------------------------------------------------------
-app.use((req, res) => {
-  res.status(404).json({ error: `Route ${req.method} ${req.originalUrl} not found` });
+  const deleted = tasks.splice(index, 1)[0];
+  res.status(200).json(deleted);
 });
 
 app.listen(PORT, () => {
-  console.log(`Task API listening on http://localhost:${PORT}`);
-  console.log(`Swagger docs at      http://localhost:${PORT}/docs`);
+  console.log(`AI-version Task API running on http://localhost:${PORT}`);
 });
